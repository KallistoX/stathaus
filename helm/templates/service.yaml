apiVersion: v1
kind: Service
metadata:
  name: {{ include "stathaus.fullname" . }}
  labels:
    {{- include "stathaus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stathaus.selectorLabels" . | nindent 4 }}
